{
	"name": "server",
	"version": "1.0.0",
	"description": "",
	"main": "dist/index.js",
	"scripts": {
		"dev": "concurrently -c 'bgBlue.bold,bgGreen.bold' -k \"tsc -w\" \"serverless offline --reloadHandler\"",
		"compile:watch": "tsc -w",
		"serverless-offline": "serverless offline start",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"body-parser": "^1.20.2",
		"cors": "^2.8.5",
		"express": "^4.18.2",
		"serverless-http": "^3.2.0"
	},
	"devDependencies": {
		"@types/cors": "^2.8.14",
		"@types/express": "^4.17.18",
		"concurrently": "^8.2.1",
		"serverless": "^3.35.2",
		"serverless-offline": "^13.1.2",
		"typescript": "^5.2.2"
	}
}
